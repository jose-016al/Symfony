<form method="post" action="{{ path('app_articulos_delete', {'id': articulo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ articulo.id) }}">
    <button class="btn bg-danger text-white">Eliminar</button>
</form>
