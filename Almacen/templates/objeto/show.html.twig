{% extends 'base.html.twig' %}

{% block title %}Objeto{% endblock %}

{% block body %}
    <h1>Objeto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ objeto.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ objeto.descripcion }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ objeto.cantidad }}</td>
            </tr>
            <tr>
                <th>Ubicacion</th>
                <td>{{ objeto.ubicacion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_objeto_index') }}">back to list</a>

    <a href="{{ path('app_objeto_edit', {'id': objeto.id}) }}">edit</a>

    {{ include('objeto/_delete_form.html.twig') }}
{% endblock %}
