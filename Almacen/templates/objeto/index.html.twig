{% extends 'base.html.twig' %}

{% block title %}Objeto index{% endblock %}

{% block body %}
    <h1>Objeto index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>Cantidad</th>
                <th>Ubicacion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for objeto in objetos %}
            <tr>
                <td>{{ objeto.id }}</td>
                <td>{{ objeto.descripcion }}</td>
                <td>{{ objeto.cantidad }}</td>
                <td>{{ objeto.ubicacion }}</td>
                <td>
                    <a href="{{ path('app_objeto_show', {'id': objeto.id}) }}">show</a>
                    <a href="{{ path('app_objeto_edit', {'id': objeto.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_objeto_new') }}">Create new</a>
{% endblock %}
