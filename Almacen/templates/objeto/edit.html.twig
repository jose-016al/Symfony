{% extends 'base.html.twig' %}

{% block title %}Edit Objeto{% endblock %}

{% block body %}
    <h1>Edit Objeto</h1>

    {{ include('objeto/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_objeto_index') }}">back to list</a>

    {{ include('objeto/_delete_form.html.twig') }}
{% endblock %}
