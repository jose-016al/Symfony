{% extends 'base.html.twig' %}

{% block title %}Entrada index{% endblock %}

{% block body %}
    <h1>Entrada index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fecha</th>
                <th>Cantidad</th>
                <th>Objeto</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entrada in entradas %}
            <tr>
                <td>{{ entrada.id }}</td>
                <td>{{ entrada.fecha ? entrada.fecha|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ entrada.cantidad }}</td>
                <td>{{ entrada.objeto.descripcion }}</td>
                <td>
                    <a href="{{ path('app_entrada_show', {'id': entrada.id}) }}">show</a>
                    <a href="{{ path('app_entrada_edit', {'id': entrada.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_entrada_new') }}">Create new</a>
{% endblock %}
