<form method="post" action="{{ path('app_entrada_delete', {'id': entrada.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entrada.id) }}">
    <button class="btn">Delete</button>
</form>
