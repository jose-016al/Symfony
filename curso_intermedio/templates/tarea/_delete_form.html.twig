<form method="post" action="{{ path('app_tarea_delete', {'id': tarea.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tarea.id) }}">
    <button class="btn">Delete</button>
</form>
